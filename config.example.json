{
  "gopay": {
    "country_code": "62",
    "phone_number": "default phone number without country code",
    "pin": "shared 6-digit PIN",
    "browser_locale": "zh-CN",
    "pin_locale": "id",
    "otp_channel": "whatsapp",
    "___otp_channel_options___": "whatsapp (default) | sms; when sms is selected, the script waits for the countdown and then switches OTP delivery to SMS",
    "sms_switch_countdown_sec": 30,
    "sms_switch_endpoint": "",
    "___sms_switch_endpoint_hint___": "Leave empty to use the built-in default. If GoPay changes the API, capture a HAR and put the exact URL here.",
    "sms_switch_body_extra": {}
  },
  "proxy": "socks5://127.0.0.1:1080",
  "orchestrator": {
    "port": 8800,
    "otp_timeout": 90,
    "___otp_timeout_tip___": "For SMS mode, 120+ seconds is recommended because the flow waits for the 30-second countdown after consent, switches to SMS, and then waits for the SMS platform to receive the message.",
    "auth_token": "custom random string"
  },
  "otp": {
    "mode": "manual",
    "___mode_options___": "manual (POST /otp manually) | sms_api (poll an SMS receiving platform) | whatsapp (WhatsApp gRPC)",
    "___sms_tips___": "If using sms_api, it is strongly recommended to also set gopay.otp_channel='sms'; otherwise the code will be sent to WhatsApp and the SMS platform will not receive it.",
    "sms_api": {
      "provider": "herosms",
      "api_key": "",
      "base_url": "https://api.herosms.com",
      "country": "id",
      "service": "gopay",
      "poll_interval_sec": 3,
      "poll_timeout_sec": 90
    },
    "whatsapp": {
      "grpc_addr": "127.0.0.1:50056"
    }
  }
}